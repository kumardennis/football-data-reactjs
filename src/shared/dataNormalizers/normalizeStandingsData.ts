import { StandingsTableDataType } from "src/features/CompetitionStandings/types/standingTables.types";

export const normalizeStandingsData = (
  response: any
): StandingsTableDataType[] => {
  const normalizedData: StandingsTableDataType[] = [];

  //TODO: normalizeData with response

  return normalizedData;
};
